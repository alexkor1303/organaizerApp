import React from "react";

type WeatherProps = {
  className: string;
};

export const Weather = async ({ className }: WeatherProps) => {
  return (
    <div>
      <h1>weather infograpgh</h1>
    </div>
  );
};
